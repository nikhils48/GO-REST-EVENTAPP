FROM golang:1.25-alpine

# Install git for Go modules
RUN apk add --no-cache git

# Install stable Air version (avoids huge Hugo deps)
RUN go install github.com/air-verse/air@latest

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

EXPOSE 8081
CMD ["air", "-c", ".air.toml"]
